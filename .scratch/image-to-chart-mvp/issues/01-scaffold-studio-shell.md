# 01 — Scaffold the local-first Studio shell

**What to build:** A Knitter can open a responsive PWA shell, create an empty Pattern Project, and see the three-column Studio with persistent undo/redo/export placeholders and an empty local project list. Pattern Projects and Yarn Inventory round-trip through IndexedDB with a schema version.

**Blocked by:** None — can start immediately.

**Status:** resolved

- [x] Creating a Pattern Project works without an account
- [x] Studio shows left controls, central chart area, and right color-key area
- [x] IndexedDB stores and reloads project and inventory schema versions
- [x] Local-only storage warning is visible near project management
- [x] App shell installs/caches as a progressive enhancement without blocking tab use

## Notes

Delivered in commit `ca9c7e8`. Local repository seam and Studio shell journey tests cover create/list/reload. PWA service worker is generated by `vite-plugin-pwa` on production build.
